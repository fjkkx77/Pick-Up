# 接着来

> **在线使用：https://pick-up-inky.vercel.app/**
> （自有域名 `jzl.wbztl.xyz` 已在 Vercel 上加好，等 DNSPod 加一条 CNAME 就能用，见文末）

追的剧、在学的课、做了一半的活，**一次做不完，停下来就容易忘了停在哪、当时想到哪、甚至忘了这件事**。
这个网站只干一件事：每件事记下「停在哪 + 下次从哪接着来」，下次打开就能无缝接上。

---

## 一、怎么用

1. 点右下角 **＋**，写名字、选一种算法（追剧 / 读书 / 网课 / 刷题 / 任务 / 其它），点「建好」。
2. 建好后直接进详情：调到你现在的位置（比如第 1 季第 7 集、23:14），写一句「下次从哪接着来」，点 **停在这里**。
3. 以后每次：
   - **赶时间** → 首页卡片右边点 **+1**，2 秒记完（看完一集就点一下）
   - **想多记两句** → 点卡片进详情，调位置、写笔记、停在这里
4. 下次打开，卡片上直接写着停在哪 + 上次写的那句话。

| 你遇到的情况 | 它怎么帮你 |
|---|---|
| 忘了看到第几集 / 第几页 / 几分几秒 | 卡片上大字写着位置，视频还能记到秒 |
| 忘了当时想到哪、下一步要干嘛 | 每次停下写的那句话，显示在卡片上和详情最上面 |
| 忘了这件事还没做完 | 超过 14 天没碰的会在首页顶上提醒你（天数可以在设置里改） |
| 嫌记录麻烦 | 首页一下 +1；点错了提示条上点「撤销」 |

### 几种算法

| 选这个 | 记什么 | 例子 |
|---|---|---|
| 追剧 | 第几季 · 第几集 · 几分几秒 | 漫长的季节 第 1 季第 7 集 23:14 |
| 读书 | 第几章 · 第几页 | 概率论 第 3 章第 42 页 |
| 网课 | 第几节 · 几分几秒 | Python 第 12 节 8:30 |
| 刷题 | 第几题 | 数据结构 第 37/200 题 |
| 任务 | 拆成几步，做完一步勾一步 | 写周报：收集数据 ✓ 写初稿 ✓ 发出去 ☐ |
| 其它 | 不计数，只记一句话 | 随便什么说不清单位的事 |

建好之后单位名、层数都能改（详情最下面「设置」）。**总数不知道可以空着**（连载中的剧）。

### 一些细节

- **+1 到头了**：
  - 这一季看完、但不知道还有没有下一季 → 不替你猜，先标成「等更新」，提示条上有「进入下一季」按钮
  - 全部看完 → 标成「等更新」，提示条上有「标记为看完了」；**不会自动算完成**
  - 剧更新了、你把总集数改大 → 自动回到「进行中」
- **时间点**直接打数字：`2314` = 23:14，`10530` = 1:05:30，也可以打 `23:14`
- **撤销**：+1 之后 5 秒内点提示条的「撤销」；过了也行，详情里的记录列表能删单条
- **左滑卡片**：暂停 / 完成 / 删除（删除会先问你）
- **二刷**：详情里「从头再来一遍」，一刷的记录留着
- **状态**：进行中 / 等更新 / 暂停 / 已完成 / 弃坑；后两种在「归档」里
- **速度估计**：记录够多时，详情里会显示「最近每天约 N 集，照这个速度约 M 天做完」
- **链接**：详情里存一个链接，「接着来」卡片上会出现「去看 ↗」
- **往下拉**：首页往下拉 = 同步 + 刷新；弹出的面板往下拉 = 关掉（调过的东西没保存会先问）

## 二、手机和电脑用同一份数据

右上角 ⚙ → **多设备同步** → 点「生成」→「开启同步」。然后在另一台设备上：

- **扫码**：用手机相机扫设置里的二维码，打开后点「接入」
- 或者点「复制接入链接」发给自己，在另一台设备打开

之后两边自动互通（改完 0.6 秒上传；切回页面、每 30 秒、下拉都会拉取）。

- 同步码相当于密码，知道它的人能看到并改你的记录，别外传；服务器上只存它的哈希
- 两台设备离线时各记了一次，联网后**两条都在**，不会互相覆盖（当前位置取时间较新的那条）
- 删掉的东西会同步删除，不会被另一台设备加回来

## 三、数据存在哪

- **没开同步**：只在当前这个浏览器里。清缓存、换浏览器 → 会丢。
- **开了同步**：本机 + 云端（Upstash Redis）。换设备填同一个码就能取回。
- 不管开没开，都建议偶尔 ⚙ → **导出备份** 存一份 `.json`；导入是**合并**，不会覆盖现有的。

---

## 四、已验证 / 未验证

**2026-09-24 已验证**（headless Chrome，真实窄屏视口 + CDP 真实触摸）：

- 数据逻辑 36 条自检（`node tests/model.test.js`，或双击 `tests/model.html`）；另外故意改坏 8 处核心逻辑，每处都有用例抓到
- 功能流程 27 条：新建自动聚焦、停在这里、+1、撤销、写两句、到头变等更新、进入下一季、清单、左滑暂停、下拉关闭、改过再拉会先问、删除确认带名字
- 两台设备同步 13 条（本地 + **线上真实 Redis 各跑一遍**）：扫码链接先确认再接入、地址栏里的码会被抹掉、离线各记一条合并后都在、删除不回流
- 布局：320 / 390 / 430 / 1707 × 浅色 / 深色，首页、详情、清单详情、新建、设置（开同步前后）、提示条、四个标签页：无横向溢出、没有元素伸出屏幕、可点元素都 ≥44px；截图人工看过
- 服务端没配存储时显示中文说明，不报错
- 线上：`.env.local`、测试脚本、设计文档都是 404

**没验证、需要你在真机上确认**：

- iPhone 上点 ＋ / 写两句后**键盘是否直接弹出**（代码按已知的 iOS 要求写了，headless 验不了）
- 左滑、下拉关闭面板、首页下拉刷新的**手感**
- 手机相机**扫设置里的二维码**能否直接打开并接入（生成的码本身验过能出图，没用真相机扫）
- 手机**不挂代理**能不能打开 `*.vercel.app`（Vercel 官方不保证中国大陆访问；自有域名配好后概率会好一些，但也没有保证）

---

## 五、开发

纯静态前端 + 一个 Vercel 函数，没有构建步骤。

| 文件 | 干什么 |
|---|---|
| `assets/model.js` | 数据逻辑（纯函数，有自检）：+1 规则、当前位置、速度、冷落、撤销 |
| `assets/store.js` | 本地存储 + 同步调度（防抖、切走立刻传、每 30 秒） |
| `assets/app.js` / `app.css` / `index.html` | 界面 |
| `assets/sync-core.js` | 多设备同步内核（取自日程卡片，逐条合并 + 墓碑删除） |
| `assets/gestures.js` / `ptr.js` / `qrcode.js` | 左滑与下拉关闭 / 下拉刷新 / 二维码（均取自已验证的组件） |
| `api/sync.js` | 同步接口（Upstash Redis，零依赖） |

**核心设计**：位置不存在「这件事」上，而是由「最新一条记录」推出来。每次 +1、每次停在这里都**追加**一条记录，
从不修改旧的——所以两台设备离线各记一次，合并就是取并集，谁也不覆盖谁。详见 `docs/superpowers/specs/`。

```bash
node tests/model.test.js                       # 数据逻辑自检
node tests/ui/mock.js "$PWD" 8841              # 本地服务（跑真实 api/sync.js + 内存 Redis 替身）
node tests/ui/flow.js                          # 端到端：功能 + 同步 + 布局（约 6 分钟）
BASE=https://pick-up-inky.vercel.app/ node tests/ui/flow.js sync   # 对线上跑同步
```

部署：Vercel 项目 `pick-up` 已关联本仓库，`git push` 自动部署。
环境变量 `KV_REST_API_URL` / `KV_REST_API_TOKEN` 和日程卡片共用同一个 Upstash 库（键前缀不同，互不串数据）。

### 自有域名（待办）

在 DNSPod 给 `wbztl.xyz` 加一条：

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| `jzl` | CNAME | `cdb929a3f1cb535c.vercel-dns-017.com` |

加好后跑 `vercel domains verify jzl.wbztl.xyz` 确认。
